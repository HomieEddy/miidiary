---
phase: 02-encrypted-storage-basic-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/entry.ts
  - src/models/EntryRealm.ts
  - src/utils/entryTextDerivation.ts
  - src/services/secureStorageService.ts
  - src/services/keychainService.ts
  - src/services/realmService.ts
  - src/components/ui/BiometricGate.tsx
  - src/tests/entryTextDerivation.test.ts
  - src/tests/realmService.test.ts
  - src/tests/biometricGate.test.tsx
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03, STOR-06, SEC-01, SEC-02]
user_setup: []

must_haves:
  truths:
    - "Realm opens with encryption key resolved through MMKV + Keychain and fails closed if key resolution is invalid"
    - "Entry schema persists first-class fields (id, createdAt, updatedAt, text, category) and derived fields (title, previewText) per D-06 and D-07"
    - "title and previewText are derived deterministically from source text per D-08"
    - "A deterministic single indexed queryKey supports chronological/category query performance targeting STOR-06"
    - "Cold launch is protected by biometric unlock with device passcode fallback per D-04 and D-05"
    - "No cloud dependency is introduced in storage or biometric setup paths per D-03"
  artifacts:
    - path: "src/models/EntryRealm.ts"
      provides: "Encrypted Realm Entry schema with indexed deterministic queryKey"
      exports: ["EntryRealmSchema", "buildEntryQueryKey"]
    - path: "src/services/realmService.ts"
      provides: "Encrypted Realm open/close and key resolution wiring"
      exports: ["getRealmInstance", "closeRealmInstance"]
    - path: "src/services/keychainService.ts"
      provides: "Keychain key lifecycle for Realm encryption key"
      exports: ["getRealmKey", "setRealmKey", "resetRealmKey"]
    - path: "src/services/secureStorageService.ts"
      provides: "MMKV metadata namespace for key reference/version data"
      exports: ["secureStorage", "getRealmKeyMetadata", "setRealmKeyMetadata"]
    - path: "src/components/ui/BiometricGate.tsx"
      provides: "Biometric cold-start gate shell using expo-local-authentication"
      exports: ["BiometricGate"]
  key_links:
    - from: "src/services/realmService.ts"
      to: "src/services/keychainService.ts"
      via: "realm encryption key fetch/generate lifecycle"
      pattern: "getRealmKey|setRealmKey"
    - from: "src/services/realmService.ts"
      to: "src/services/secureStorageService.ts"
      via: "key metadata read/write for deterministic key reference"
      pattern: "getRealmKeyMetadata|setRealmKeyMetadata"
    - from: "src/models/EntryRealm.ts"
      to: "src/utils/entryTextDerivation.ts"
      via: "create/update flows derive title and previewText before persistence"
      pattern: "deriveEntryTitle|deriveEntryPreview"
    - from: "src/app/_layout.tsx"
      to: "src/components/ui/BiometricGate.tsx"
      via: "root-level cold-launch lock before tab tree mounts"
      pattern: "BiometricGate"
---

<objective>
Create the encrypted persistence and security foundation for Phase 2: Realm schema with deterministic indexing, MMKV + Keychain key management, deterministic title/preview derivation, and a cold-launch biometric gate with passcode fallback.

Purpose: This plan establishes the locked Phase 2 foundations (D-01 through D-08, D-04, D-05) so all later CRUD and browse features run on secure, deterministic, offline-only infrastructure.
Output: Encrypted Realm boot path, deterministic schema/index contracts, and root biometric gate wiring are in place and testable.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-encrypted-storage-basic-browse/02-CONTEXT.md
@.planning/phases/02-encrypted-storage-basic-browse/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@AGENTS.md

<interfaces>
From 02-CONTEXT.md locked decisions:
- D-01 Realm is mandatory storage engine.
- D-02 Key flow locked to MMKV + Keychain.
- D-03 Fully offline behavior is mandatory.
- D-04 Biometric unlock is required on cold launch.
- D-05 Biometric flow must permit device passcode fallback.
- D-06 Persist id, createdAt, updatedAt, text, category.
- D-07 Persist title and previewText as derived display fields.
- D-08 Derive title from first sentence and truncate preview deterministically.

From AGENTS.md architecture lock:
- Persistence: @realm/react
- Secure storage: react-native-mmkv + react-native-keychain
- Biometrics: expo-local-authentication
- No cloud features or async-storage substitutions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define secure entry contracts, Realm schema, and deterministic derivation utilities</name>
  <files>src/types/entry.ts, src/models/EntryRealm.ts, src/utils/entryTextDerivation.ts, src/tests/entryTextDerivation.test.ts</files>
  <behavior>
    - Test 1: Identical source text always yields the same title/preview output.
    - Test 2: title is first-sentence based and preview truncates with fixed deterministic limits.
    - Test 3: queryKey generation is deterministic for identical category+timestamp+id input.
  </behavior>
  <action>
    Implement D-06, D-07, D-08 by introducing canonical entry types and a Realm schema that includes id, createdAt, updatedAt, text, category, title, previewText, and indexed queryKey.
    Create a deterministic queryKey builder in `src/models/EntryRealm.ts` using `category|reverseEpochMs|id` format (fixed-width reverse timestamp) for STOR-06.
    Add `entryTextDerivation` helpers with fixed max-length constants for title and preview, preserving strict deterministic behavior.
    Do not add any cloud, network, or non-Realm storage code (D-03).
  </action>
  <verify>
    <automated>npm run test -- src/tests/entryTextDerivation.test.ts -i</automated>
  </verify>
  <done>
    Realm schema contract exists with required fields and indexed deterministic queryKey.
    Derivation utilities are deterministic and covered by tests.
    Entry domain types are reusable for repository and browse layers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement encrypted Realm boot with MMKV + Keychain key lifecycle</name>
  <files>src/services/secureStorageService.ts, src/services/keychainService.ts, src/services/realmService.ts, src/tests/realmService.test.ts</files>
  <behavior>
    - Test 1: missing key metadata resolves to key generation then persisted Keychain+MMKV references.
    - Test 2: existing key metadata path reuses stored key and opens Realm encrypted.
    - Test 3: key mismatch/error path fails closed and does not open unencrypted realm.
  </behavior>
  <action>
    Implement D-01 and D-02 by creating secure storage and keychain services for Realm key lifecycle, then wire encrypted Realm opening in `realmService` with schemaVersion and migration callback.
    Ensure key material authority remains Keychain while MMKV stores reference/version metadata only.
    Explicitly disallow destructive migration shortcuts and preserve offline-only operation per D-03.
    Include deterministic index registration for queryKey and expose minimal service APIs needed by later repository work.
  </action>
  <verify>
    <automated>npm run test -- src/tests/realmService.test.ts -i</automated>
  </verify>
  <done>
    Realm opens encrypted using keychain-resolved key lifecycle.
    MMKV metadata and Keychain key flow are deterministic and test-covered.
    Service API is ready for repository CRUD integration.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add root biometric cold-launch gate with passcode fallback foundation</name>
  <files>src/components/ui/BiometricGate.tsx, src/app/_layout.tsx, src/tests/biometricGate.test.tsx</files>
  <behavior>
    - Test 1: cold launch blocks protected app shell until successful local authentication.
    - Test 2: unavailable biometrics uses passcode-capable fallback path.
    - Test 3: failed/cancelled auth keeps lock screen visible and allows retry.
  </behavior>
  <action>
    Implement D-04 and D-05 via a `BiometricGate` component using expo-local-authentication checks (`hasHardwareAsync`, `isEnrolledAsync`, `authenticateAsync`) with device fallback enabled.
    Mount the gate in root app layout so tabs/screens do not render before unlock on cold launch.
    Keep this as foundation-only gate logic; do not add resume-lock policy beyond cold launch.
  </action>
  <verify>
    <automated>npm run test -- src/tests/biometricGate.test.tsx -i</automated>
  </verify>
  <done>
    Cold launch authentication gate exists and is wired before app content mounts.
    Biometric + passcode fallback behavior aligns with SEC-01 and SEC-02.
  </done>
</task>

</tasks>

<verification>
- Run `npm run test -- src/tests/entryTextDerivation.test.ts src/tests/realmService.test.ts src/tests/biometricGate.test.tsx -i`.
- Run `npm run lint` and ensure no type or import-order regressions.
- Confirm no network client/service was introduced in modified files.
</verification>

<success_criteria>
- STOR-02 and STOR-06 are fully implemented with deterministic test coverage.
- SEC-01 and SEC-02 foundation is fully implemented at app root.
- STOR-01 and STOR-03 foundations are in place for repository and browse consumption in Plan 02-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-encrypted-storage-basic-browse/02-01-SUMMARY.md`.
</output>
