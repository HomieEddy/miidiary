---
phase: 03-on-device-ml-pipeline
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/hooks/useTranscription.ts
  - src/services/audioCaptureService.ts
  - src/services/entriesRepository.ts
  - src/tests/useTranscription.test.ts
autonomous: true
requirements: [TRAN-06, BACK-01, BACK-02]
user_setup: []
---

# Plan 03-03: Pipeline Orchestration and Reliability

## Objective
Harden end-to-end pipeline (record -> transcribe -> classify -> persist -> cleanup) with strict stage isolation and ephemeral-audio guarantees.

## Tasks

1. Refactor orchestration into explicit stage transitions with stage-specific error handling.
2. Enforce raw audio deletion after successful or terminal processing outcomes.
3. Add best-effort background completion hooks for queued transcriptions.
4. Add tests for cleanup guarantees, retry semantics, and partial-stage failures.

## Verification

- `npm run test -- src/tests/useTranscription.test.ts src/tests/useAudioCapture.test.ts -i`
- Manual: kill/restart app during processing and validate graceful recovery behavior.

## Success Criteria

- TRAN-06 satisfied (ephemeral audio).
- BACK-01/BACK-02 baseline orchestration hooks implemented for deferred completion behavior.
