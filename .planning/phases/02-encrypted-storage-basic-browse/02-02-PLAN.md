---
phase: 02-encrypted-storage-basic-browse
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/services/entriesRepository.ts
  - src/services/localAuthService.ts
  - src/hooks/useEntries.ts
  - src/utils/entryGrouping.ts
  - src/hooks/useTranscription.ts
  - src/components/ui/DeleteModeToolbar.tsx
  - src/screens/DiaryScreen.tsx
  - src/tests/entriesRepository.test.ts
  - src/tests/entryGrouping.test.ts
  - src/tests/DiaryScreen.test.tsx
autonomous: true
requirements: [STOR-01, STOR-04, STOR-05, BROW-01]
user_setup: []

must_haves:
  truths:
    - "Entries created from transcription flow persist to encrypted Realm and survive restart"
    - "Diary screen renders day-grouped chronological FlashList rows with category badge, title, one-line preview, and timestamp per D-09 and D-10"
    - "Long-press enters delete mode, X affordance triggers confirm dialog, and confirmed delete removes exactly one entry per D-11"
    - "Wipe-all requires double confirmation and biometric re-auth before destructive execution per D-12"
    - "Offline/airplane-mode behavior preserves full create/list/delete/wipe usability with no network requirement"
  artifacts:
    - path: "src/services/entriesRepository.ts"
      provides: "Realm-backed entry CRUD and destructive operations"
      exports: ["entriesRepository"]
    - path: "src/hooks/useEntries.ts"
      provides: "UI adapter for grouped browse data + delete/wipe commands"
      exports: ["useEntries"]
    - path: "src/utils/entryGrouping.ts"
      provides: "Deterministic day-section flattening for FlashList item model"
      exports: ["groupEntriesByDay", "flattenEntrySections"]
    - path: "src/screens/DiaryScreen.tsx"
      provides: "FlashList chronological browse + delete mode + wipe-all UX"
      contains: "FlashList"
  key_links:
    - from: "src/hooks/useTranscription.ts"
      to: "src/services/entriesRepository.ts"
      via: "transcription result persistence handoff"
      pattern: "createEntry"
    - from: "src/screens/DiaryScreen.tsx"
      to: "src/hooks/useEntries.ts"
      via: "read model + delete/wipe actions"
      pattern: "useEntries"
    - from: "src/screens/DiaryScreen.tsx"
      to: "@shopify/flash-list"
      via: "flattened grouped data with getItemType"
      pattern: "FlashList|getItemType"
    - from: "src/hooks/useEntries.ts"
      to: "src/services/localAuthService.ts"
      via: "wipe-all re-auth mediated by hook/service layer before destructive transaction"
      pattern: "reauthenticateForDestructiveAction"
---

<objective>
Deliver the Phase 2 user-facing experience on top of encrypted foundations: repository CRUD, chronological FlashList browse, delete-one flow, wipe-all flow with biometric re-auth, and explicit offline validation behavior.

Purpose: This plan completes the core storage+browse outcomes users experience directly while honoring locked destructive-flow safety decisions (D-11, D-12) and browse presentation decisions (D-09, D-10).
Output: End-to-end offline encrypted entry persistence, chronological browsing, and destructive actions are functional and testable.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-encrypted-storage-basic-browse/02-CONTEXT.md
@.planning/phases/02-encrypted-storage-basic-browse/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.planning/phases/02-encrypted-storage-basic-browse/02-01-PLAN.md

<interfaces>
Dependencies expected from Plan 02-01:
- Encrypted Realm service open/close API
- Entry schema contract with deterministic queryKey
- Deterministic `title`/`previewText` derivation utilities
- Root biometric gate in app layout

Locked UX decisions from 02-CONTEXT.md implemented here:
- D-09 day-grouped chronological sections
- D-10 row fields: category badge + title + one-line preview + timestamp
- D-11 long-press delete mode then X-confirm flow
- D-12 wipe-all double confirm + biometric re-auth
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Realm-backed entry repository and hook transcription persistence into it</name>
  <files>src/services/entriesRepository.ts, src/hooks/useTranscription.ts, src/tests/entriesRepository.test.ts</files>
  <behavior>
    - Test 1: createEntry persists required and derived fields and returns persisted entity.
    - Test 2: listEntriesChronological returns newest-first deterministically.
    - Test 3: deleteEntry removes only targeted id; wipeAll removes all entries.
    - Test 4: create entry, close Realm instance, reopen Realm instance, assert entry still exists.
  </behavior>
  <action>
    Implement STOR-01 and STOR-04 behavior by creating a repository layer over encrypted Realm from Plan 02-01.
    Replace in-memory entry writes in `useTranscription` with repository `createEntry` calls.
    Keep persistent source-of-truth in Realm only; do not introduce a duplicated persistent Zustand array.
    Keep repository API minimal and explicit: create, listChronological, deleteOne, wipeAll, count.
  </action>
  <verify>
    <automated>npm run test -- src/tests/entriesRepository.test.ts -i</automated>
  </verify>
  <done>
    Persistence path is repository-driven and survives explicit close/reopen restart test.
    No persistent data flow depends on network or Zustand memory cache.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement day-grouped FlashList browse UI and rendering primitives</name>
  <files>src/utils/entryGrouping.ts, src/hooks/useEntries.ts, src/screens/DiaryScreen.tsx, src/tests/entryGrouping.test.ts, src/tests/DiaryScreen.test.tsx</files>
  <behavior>
    - Test 1: grouping utility emits deterministic day buckets and stable flattened keys.
    - Test 2: Diary screen renders headers and rows in descending chronological order.
    - Test 3: each row shows category badge, title, one-line preview, and timestamp.
  </behavior>
  <action>
    Implement BROW-01 and D-09/D-10 using FlashList with flattened section model (`header` and `row` item types) and `getItemType` for recycling.
    Implement reusable browse rendering inside `DiaryScreen` for this phase scope while keeping component boundaries clean and thin.
    Keep `DiaryScreen` thin by sourcing data/actions from `useEntries` hook only.
    Use deterministic date-label logic (Today, Yesterday, older date labels) without introducing search/filter/edit scope.
  </action>
  <verify>
    <automated>npm run test -- src/tests/entryGrouping.test.ts src/tests/DiaryScreen.test.tsx -i</automated>
  </verify>
  <done>
    Chronological grouped browse works through FlashList with stable render model.
    Row content contract exactly matches D-10.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add delete-one and wipe-all safety flows with offline validation checks</name>
  <files>src/components/ui/DeleteModeToolbar.tsx, src/hooks/useEntries.ts, src/services/localAuthService.ts, src/screens/DiaryScreen.tsx, src/tests/DiaryScreen.test.tsx, src/tests/entriesRepository.test.ts</files>
  <behavior>
    - Test 1: long-press enters delete mode and X action prompts single-delete confirmation.
    - Test 2: confirmed single delete removes exactly one row and exits pending state safely.
    - Test 3: wipe-all requires two confirmations and successful biometric re-auth before delete.
  </behavior>
  <action>
    Implement STOR-05 with D-11 and D-12 exactly: long-press to enter delete mode, X affordance, confirm dialog for single deletion, and wipe-all double confirm plus biometric re-auth before repository wipe.
    Keep biometric re-auth out of UI components: `DeleteModeToolbar` emits intent only; `useEntries` calls `localAuthService` and only then executes repository wipe.
    Add offline validation checks with explicit network-failure injection: mock global `fetch` to reject and assert create/list/delete/wipe flows still pass and never depend on network success.
    Keep destructive operations idempotent and error-safe at repository/hook level.
  </action>
  <verify>
    <automated>npm run test -- src/tests/entriesRepository.test.ts src/tests/DiaryScreen.test.tsx -i</automated>
  </verify>
  <done>
    Delete-one and wipe-all safety behavior matches locked decisions.
    Offline-first operation is validated with explicit mocked network failure in automated tests for storage/browse flows.
  </done>
</task>

</tasks>

<verification>
- Run `npm run test -- src/tests/entriesRepository.test.ts src/tests/entryGrouping.test.ts src/tests/DiaryScreen.test.tsx -i`.
- Run app in airplane mode and confirm create/list/delete/wipe paths still succeed.
- Confirm no cloud/network storage endpoint is referenced in modified files.
</verification>

<success_criteria>
- STOR-04, STOR-05, and BROW-01 are fully satisfied with automated verification.
- STOR-01 persistence is fully realized end-to-end from transcription to browse.
- Locked D-09 through D-12 UX decisions are implemented without simplification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-encrypted-storage-basic-browse/02-02-SUMMARY.md`.
</output>
