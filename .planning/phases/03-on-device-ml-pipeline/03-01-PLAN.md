---
phase: 03-on-device-ml-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/transcriptionService.ts
  - src/services/modelManager.ts
  - src/hooks/useTranscription.ts
  - src/components/ui/ProcessingState.tsx
  - src/tests/transcriptionService.test.ts
autonomous: true
requirements: [TRAN-01, TRAN-02, TRAN-03, TRAN-04, TRAN-05, UX-10]
user_setup: []
---

# Plan 03-01: On-Device STT Service

## Objective
Replace stub transcription with production on-device STT service using language-specific model paths and stage-based progress signals.

## Tasks

1. Build `transcriptionService` around whisper.rn with language-specific model selection and local cache.
2. Add stage-based progress reporting (`preparing`, `transcribing`, `finalizing`) and integrate it into `useTranscription` + `ProcessingState`.
3. Ensure offline-only operation with no remote transcription calls.
4. Add tests for success, failure, model-selection fallback, and progress state transitions.

## Verification

- `npm run test -- src/tests/transcriptionService.test.ts src/tests/useTranscription.test.ts -i`
- Manual: record in airplane mode and confirm transcript is produced.

## Success Criteria

- TRAN-01/02/03/04/05 and UX-10 are satisfied for STT behavior.
- UI shows meaningful stage labels during transcription.
